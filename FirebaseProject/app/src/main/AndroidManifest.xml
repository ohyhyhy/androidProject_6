<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirebaseProject"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="Firebase_login" />
        <activity
            android:name=".SignUpActivity"
            android:label="Firebase_signup" />
        <activity
            android:name=".AddItemsActivity"
            android:label="Firebase_additems" />
        <activity
            android:name=".EditItemsActivity"
            android:label="Firebase_edititems" />
        <activity
            android:name=".ViewItemsActivity"
            android:label="Firebase_viewitems" />
        <activity
            android:name=".ViewMessageActivity"
            android:label="Firebase_viewmessages" />
    </application>

</manifest>